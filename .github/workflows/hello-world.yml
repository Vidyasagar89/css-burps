name: Hello World Workflow

on: 
    push:
        branches:
            - master

jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Echo Hello
              run: echo "Hello, GitHub Action!"
